{
  "name": "vtcar-gptmaker-integration",
  "version": "1.0.0",
  "private": true,
  "description": "Backend que recebe leads do GPT Maker (webhook) e expoe para o CRM da VT Car",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "register-webhook": "node scripts/register-webhook.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
